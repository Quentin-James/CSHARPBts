<Window x:Class="CSVParsing.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSVParsing"
        mc:Ignorable="d"
        Title="Gestion des Spectacles" Height="400" Width="700">
    <Grid Margin="10">
        <StackPanel>
            <WrapPanel Margin="0,0,0,10">
                <Button x:Name="ImportButton" 
                        Content="Importer CSV" 
                        Click="ImportCsvButton_Click" 
                        Width="120" 
                        Height="30"
                        Margin="0,0,10,0"/>
                <Button x:Name="ChevauchementButton" 
                        Content="Voir Chevauchements" 
                        Click="ChevauchementButton_Click" 
                        Width="120" 
                        Height="30"/>
            </WrapPanel>
            <ProgressBar x:Name="ProgressBar" 
                        Height="20" 
                        Margin="0,10,0,0"
                        Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
