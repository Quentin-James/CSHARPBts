<Window x:Class="CSVParsing.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSVParsing"
        mc:Ignorable="d"
        Title="Importation CSV" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button x:Name="ImportButton" 
                Content="Importer un fichier CSV" 
                Click="ImportCsvButton_Click"
                Padding="10,5"
                HorizontalAlignment="Left"/>

        <ProgressBar x:Name="ProgressBar" 
                    Grid.Row="1"
                    Visibility="Collapsed" 
                    Height="20" 
                    Margin="0,10,0,0"
                    IsIndeterminate="False"/>

        <!-- Vous pouvez ajouter d'autres contrôles ici si nécessaire -->
    </Grid>
</Window>
